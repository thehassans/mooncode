# Paste this into Plesk Git "Deploy actions" field
# Uses Node.js 24.9.0 from nodenv by adding it directly to PATH

# Set Node.js 24.9.0 path explicitly
export PATH="$HOME/.nodenv/versions/24.9.0/bin:$PATH"

# Backend deployment
cd backend
npm ci --production
pm2 restart mooncode-backend || pm2 start src/index.js --name mooncode-backend

# Frontend deployment
cd ../frontend
npm ci
npm run build

echo "✅ Deployment complete at $(date)"
